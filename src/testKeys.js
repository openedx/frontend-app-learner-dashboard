import { StrictDict } from 'utils';

export const htmlProps = StrictDict({
  disabled: 'disabled',
  href: 'href',
  onClick: 'onClick',
  onChange: 'onChange',
  onBlur: 'onBlur',
});

export default {
  htmlProps,
};

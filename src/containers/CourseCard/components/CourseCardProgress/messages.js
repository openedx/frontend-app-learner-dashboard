import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  courseProgress: {
    id: 'learner-dash.courseCard.CourseCardProgress.courseProgress',
    description: 'Course progress label on course card.',
    defaultMessage: 'Progress',
  },
  loading: {
    id: 'learner-dash.courseCard.CourseCardProgress.loading',
    description: 'Loading indicator text for course progress.',
    defaultMessage: 'loading...',
  },
});

export default messages;
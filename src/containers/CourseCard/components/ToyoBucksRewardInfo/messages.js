import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  toyoBucksClaimed: {
    id: 'learner-dash.courseCard.ToyoBucksRewardInfo.toyoBucksClaimed',
    description: 'Toyo Bucks claimed label on course card.',
    defaultMessage: '{claimed} out of {total} Toyo Bucks claimed',
  },
});

export default messages;
